#area of circle

import math
radius = float(input("Input the radius of the cicle : "))

area = math.pi * radius *radius

print("The Area of the circle with radius "+ str(radius) +" is : {0}".format(area))
